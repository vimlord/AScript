#include <stdio.h>

/**
 * Performs a PEMDAS operation based on an input.
 * calc - The calculation
 * dst  - The register where the result will be stored.
 */
void pemdas(FILE* execfile, char* calc, char* dst);
